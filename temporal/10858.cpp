#include <iostream>
#include <vector>
#include <list>
#include <tuple>
#include <stack>

using namespace std;

int main() {
    int n;

    while (cin >> n && n != 0) {
        int numero;
        int divisor = 2;
        stack<tuple<int, int, vector<int>>> trabajador;
        for(int i; i < numero; i++){
            
        }
    }

    return 0;
}
